{
  "name": "websocket",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "tsc && tsc-alias",
    "clean": "rimraf node_modules .turbo dist logs",
    "dev": "tsnd --respawn --transpile-only --watch src -r tsconfig-paths/register src/App.ts",
    "lint": "eslint . --ext .ts"
  },
  "devDependencies": {
    "@types/node": "^18.11.4",
    "eslint-config-vectier": "workspace:*",
    "ts-node-dev": "^2.0.0",
    "tsconfig": "workspace:*",
    "tsc-alias": "^1.7.0",
    "tsconfig-paths": "^4.1.0",
    "typescript": "^4.8.4"
  }
}
